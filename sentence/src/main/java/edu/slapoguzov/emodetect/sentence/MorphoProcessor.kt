package edu.slapoguzov.emodetect.sentence

import edu.slapoguzov.emodetect.sentence.model.Characteristic

class MorphoProcessor {
    fun process(token: String): List<Characteristic> {
        return emptyList()
    }
}